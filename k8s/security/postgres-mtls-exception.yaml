apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication

metadata:
  name: postgres-permissive
  namespace: default

spec:
  selector:
    matchLabels:
      app: postgres

  mtls:
    mode: PERMISSIVE