services:

# 1. Service de gestion des séries
  series-service:
    build: ./series-service
    container_name: series-service
    ports:
      - "8000:8000"
    volumes:
      - ./series-service:/app
    restart: always

# frontend
  frontend:
    image: nginx:alpine
    container_name: frontend
    ports:
      - "5500:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
